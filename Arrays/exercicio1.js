lista.unshift(2);
lista.push(3);
lista.push(4);
lista.push(6);
console.log (numeros)

const numeros = [2, 3, 4, 6];
let soma = 0;

for (let i = 0; i < numeros.length; i++) {
  soma += numeros[i];
}

console.log(soma);
