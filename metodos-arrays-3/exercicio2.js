const cidades = [
    "Salvador",
    "São Paulo",
    "Brasilia",
    "Recife",
    "Rio de Janeiro",
]
const maiorString = cidades.reduce(
	(accumulator.length, currentValue.length) => {accumulator + currentValue,
	initialValue
	});